import type { EffectAttrs } from "./types";

export const defaultEffectAttrs: EffectAttrs = {
	name: "Effect",
	kind: "blur",
	enabled: true,
	amount: undefined,
	params: undefined,
	color: undefined,
};
